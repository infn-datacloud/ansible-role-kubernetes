app:
  ingress:
    enabled: true
    hosts: 
    - "dashboard.{{ dns_name }}"
    ingressClassName: nginx}}
    issuer:
      name: {{ cluster_issuer_name }}
      scope: cluster
    tls:
      secretName: dash-selfsigned
